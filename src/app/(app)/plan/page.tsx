"use client";

import Link from "next/link";
import { CalendarDays, Check, ChevronRight, Clock3, Dumbbell, GripVertical, Plus } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { currentRecordDate } from "@/services/backend-service";
import { getWorkoutPlans, startWorkout } from "@/services/workout-service";
import type { WorkoutPlanRecord } from "@/types";
import { Badge, PageIntro, SectionHeader } from "@/components/ui";

export default function PlanPage() {
  const router = useRouter();
  const [plan, setPlan] = useState<WorkoutPlanRecord | null>(null);
  const [dayId, setDayId] = useState<string>();
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");
  useEffect(() => { void getWorkoutPlans().then(plans => { const next = plans[0] ?? null; setPlan(next); setDayId(next?.days.find(day => !day.isRestDay)?.id); }).catch(reason => setError(reason instanceof Error ? reason.message : "训练计划暂时不可用")); }, []);
  const activeDay = useMemo(() => plan?.days.find(day => day.id === dayId) ?? plan?.days.find(day => !day.isRestDay), [dayId, plan]);
  const start = async (nextDayId = activeDay?.id) => { if (!plan || !nextDayId) return; setLoading(true); setError(""); try { const session = await startWorkout({ recordDate: currentRecordDate(), planId: plan.id, planDayId: nextDayId }); router.push(`/workout/${session.id}`); } catch (reason) { setError(reason instanceof Error ? reason.message : "训练启动失败"); } finally { setLoading(false); } };
  return <><PageIntro eyebrow="每周节奏" title="训练计划" subtitle="推拉腿 + 核心 V1 · 让每一次训练都有方向。" action={<Link href="/history" className="secondary-button px-3"><CalendarDays size={18} />历史</Link>} />{error && <div className="mb-4 rounded-2xl bg-orange-50 p-4 text-sm text-orange-700">{error}</div>}<div className="app-card overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-5"><div><div className="eyebrow">当前计划</div><h2 className="mt-1 text-xl font-bold">{(plan?.name ?? "推拉腿 + 核心 V1").replace("PPL + Core", "推拉腿 + 核心")}</h2></div><button className="secondary-button min-h-10 px-3 text-sm" disabled><Plus size={16} />编辑</button></div><div className="grid grid-cols-5 gap-2 p-4">{(plan?.days ?? []).map(day => <button key={day.id} onClick={() => setDayId(day.id)} className={`rounded-2xl p-3 text-center ${day.id === activeDay?.id ? "bg-blue-600 text-white" : "bg-slate-50 text-slate-500"}`}><div className="text-xs font-semibold">第 {day.sortOrder + 1} 天</div><div className="mt-1 text-sm font-bold">{day.name.replace("Push", "推").replace("Pull", "拉").replace("Legs", "腿").replace("Core", "核心").replace("Rest", "休息")}</div></button>)}</div></div><div className="mt-6"><SectionHeader title={`今天 · ${activeDay?.name?.replace("Push", "推").replace("Pull", "拉").replace("Legs", "腿").replace("Core", "核心").replace("Rest", "休息") ?? "推"}`} action={<Badge tone="purple">{activeDay?.exercises.length ?? 0} 个动作</Badge>} /><div className="app-card divide-y divide-slate-100 overflow-hidden">{activeDay?.exercises.map(item => <button onClick={() => void start(activeDay.id)} key={item.id} className="flex w-full items-center gap-3 p-4 text-left transition hover:bg-blue-50/50"><GripVertical size={16} className="text-slate-300" /><div className="flex h-10 w-10 items-center justify-center rounded-2xl bg-blue-50 text-blue-600"><Dumbbell size={19} /></div><div className="min-w-0 flex-1"><div className="font-semibold">{item.exercise?.name}</div><div className="mt-1 text-xs text-slate-400">{item.exercise?.primaryMuscle ?? "力量训练"} · 休息 {item.restSeconds} 秒</div></div><div className="text-right"><div className="font-semibold text-blue-600">{item.targetSets} × {item.repMin}–{item.repMax}</div><ChevronRight size={17} className="ml-auto mt-1 text-slate-300" /></div></button>)}</div></div><div className="app-card mt-4 p-5"><div className="flex items-center gap-3"><div className="icon-tile bg-blue-50 text-blue-600"><Clock3 size={20} /></div><div><div className="font-semibold">训练小提示</div><div className="mt-1 text-sm text-slate-500">先完成热身，再从第一个正式组开始记录。</div></div></div><button onClick={() => void start()} disabled={loading || !activeDay} className="primary-button mt-5 w-full"><Dumbbell size={18} />{loading ? "启动中…" : "开始推训练"}</button></div><div className="mt-6"><SectionHeader title="计划原则" /><div className="grid gap-3 sm:grid-cols-3"><Info title="保持顺序" text="动作顺序影响今天的发力状态。" icon={<GripVertical size={18} />} /><Info title="记录每组" text="重量、次数与剩余次数都会成为下一次提示。" icon={<Check size={18} />} /><Info title="只影响未来" text="修改计划不会改变历史训练快照。" icon={<Clock3 size={18} />} /></div></div></>;
}
function Info({ title, text, icon }: { title: string; text: string; icon: React.ReactNode }) { return <div className="app-card p-4"><div className="icon-tile h-9 w-9 bg-blue-50 text-blue-600">{icon}</div><div className="mt-3 font-semibold">{title}</div><div className="mt-1 text-xs leading-5 text-slate-500">{text}</div></div>; }
