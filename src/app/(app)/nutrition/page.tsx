"use client";

import Link from "next/link";
import { CalendarDays, Camera, ChevronRight, Layers3, Plus, Search, Utensils } from "lucide-react";
import { useMemo, useState } from "react";
import { foods, nutritionGoal } from "@/mock/data";
import { useAppState } from "@/components/app-state";
import { Badge, PageIntro, ProgressBar, SectionHeader } from "@/components/ui";
import type { MealType } from "@/types";

const tabs: { key: MealType; label: string }[] = [{ key: "breakfast", label: "早餐" }, { key: "lunch", label: "午餐" }, { key: "dinner", label: "晚餐" }, { key: "snack", label: "加餐" }];
export default function NutritionPage() { const { meals, addMeal } = useAppState(); const [tab, setTab] = useState<MealType>("breakfast"); const [search, setSearch] = useState(""); const total = useMemo(() => meals.reduce((acc, meal) => ({ calories: acc.calories + meal.totals.calories, protein: acc.protein + meal.totals.protein, carbs: acc.carbs + meal.totals.carbs, fat: acc.fat + meal.totals.fat, fiber: acc.fiber + meal.totals.fiber }), { calories: 0, protein: 0, carbs: 0, fat: 0, fiber: 0 }), [meals]); const addFood = (foodName: string) => { const food = foods.find(item => item.name === foodName); if (!food) return; addMeal({ id: `meal-${Date.now()}`, type: tab, label: tabs.find(item => item.key === tab)?.label ?? "加餐", time: new Date().toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit" }), description: food.name, totals: { calories: Math.round(food.calories / 2), protein: Math.round(food.protein / 2), carbs: Math.round(food.carbs / 2), fat: Math.round(food.fat / 2), fiber: Math.round(food.fiber / 2) }, items: [{ name: food.name, amount: food.basis, calories: food.calories, protein: food.protein, carbs: food.carbs, fat: food.fat, fiber: food.fiber }] }); }; const filteredFoods = foods.filter(food => food.name.includes(search)); return <><PageIntro eyebrow="Nutrition tracking" title="饮食记录" subtitle="记录每一顿，热量与营养才真正可追踪。" action={<Link href="/nutrition/add" className="primary-button px-4"><Plus size={18} />添加</Link>} /><div className="app-card mb-4 flex items-center justify-between p-4"><div className="flex items-center gap-3"><CalendarDays size={21} className="text-blue-600" /><span className="text-lg font-bold">今天 · 8 月 17 日</span></div><ChevronRight size={19} className="text-slate-300" /></div><div className="app-card mb-4 p-5"><div className="flex items-end justify-between"><div><div className="text-sm text-slate-500">总热量</div><div className="mt-1"><span className="text-4xl font-bold text-blue-600">{total.calories}</span><span className="ml-2 text-sm text-slate-400">/ {nutritionGoal.calories} kcal</span></div></div><Badge tone="green">还可摄入 {Math.max(0, nutritionGoal.calories - total.calories)} kcal</Badge></div><div className="mt-4"><ProgressBar value={total.calories / nutritionGoal.calories * 100} /></div><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-4"><NutritionMetric label="蛋白质" value={total.protein} goal={nutritionGoal.protein} unit="g" tone="green" /><NutritionMetric label="碳水" value={total.carbs} goal={nutritionGoal.carbs} unit="g" tone="blue" /><NutritionMetric label="脂肪" value={total.fat} goal={nutritionGoal.fat} unit="g" tone="orange" /><NutritionMetric label="膳食纤维" value={total.fiber} goal={nutritionGoal.fiber} unit="g" tone="green" /></div></div><div className="app-card mb-4 overflow-hidden"><div className="grid grid-cols-4 border-b border-slate-100 p-2">{tabs.map(item => <button key={item.key} onClick={() => setTab(item.key)} className={`rounded-xl px-2 py-3 text-sm font-semibold ${tab === item.key ? "bg-blue-600 text-white shadow-sm" : "text-slate-500"}`}>{item.label}</button>)}</div><div className="divide-y divide-slate-100">{tabs.map(item => { const meal = meals.find(current => current.type === item.key); return <Link href={meal ? `/nutrition/add?meal=${meal.id}` : "/nutrition/add"} key={item.key} className={`flex items-center gap-3 p-4 ${item.key !== tab ? "opacity-70" : ""}`}><div className={`icon-tile ${item.key === "dinner" ? "bg-violet-50 text-violet-600" : "bg-blue-50 text-blue-600"}`}><Utensils size={20} /></div><div className="min-w-0 flex-1"><div className="font-semibold">{item.label}</div><div className="mt-1 truncate text-sm text-slate-500">{meal?.description ?? "待记录"}</div></div><div className="text-right">{meal ? <><div className="font-semibold text-blue-600">{meal.totals.calories} <span className="text-xs font-normal text-slate-400">kcal</span></div><div className="text-xs text-slate-400">{meal.time}</div></> : <span className="text-slate-400">—</span>}</div><ChevronRight size={18} className="text-slate-300" /></Link>; })}</div></div><div className="app-card mb-4 p-5"><SectionHeader title="常用模板" action={<button className="text-sm font-semibold text-blue-600">查看全部 <ChevronRight className="inline" size={15} /></button>} /><div className="grid grid-cols-3 gap-2">{["早餐 A", "午餐 A", "晚餐 A"].map((name, index) => <button key={name} onClick={() => addFood(index === 0 ? "红薯（蒸）" : index === 1 ? "鸡胸肉" : "鸡蛋（煮）")} className="flex flex-col items-center gap-2 rounded-2xl border border-slate-200 px-2 py-4 text-sm font-semibold transition hover:border-blue-300 hover:bg-blue-50"><Layers3 size={20} className="text-blue-600" />{name}<span className="text-xs font-normal text-slate-400">一键套用</span></button>)}</div></div><div className="app-card p-5"><SectionHeader title="食品库" action={<Link href="/nutrition/add" className="text-sm font-semibold text-blue-600">管理 <ChevronRight className="inline" size={15} /></Link>} /><div className="relative"><Search className="absolute left-4 top-3.5 text-slate-400" size={19} /><input className="field pl-11" placeholder="搜索食物，如：鸡蛋、燕麦" value={search} onChange={e => setSearch(e.target.value)} /></div><div className="mt-3 divide-y divide-slate-100">{filteredFoods.slice(0, 4).map(food => <button key={food.id} onClick={() => addFood(food.name)} className="flex w-full items-center gap-3 py-3 text-left"><div className="icon-tile h-10 w-10 rounded-xl bg-orange-50 text-orange-500"><Utensils size={18} /></div><div className="min-w-0 flex-1"><div className="font-semibold">{food.name}</div><div className="text-sm text-slate-400">{food.basis}</div></div><div className="text-right"><div className="font-semibold text-blue-600">{food.calories}</div><div className="text-xs text-slate-400">kcal / 100g</div></div><Plus size={20} className="text-blue-600" /></button>)}</div><div className="mt-4 grid grid-cols-2 gap-3"><button className="secondary-button w-full"><Camera size={18} />拍照识别</button><Link href="/nutrition/add" className="primary-button w-full"><Plus size={18} />手动添加</Link></div></div></> }
function NutritionMetric({ label, value, goal, unit, tone }: { label: string; value: number; goal: number; unit: string; tone: "green" | "blue" | "orange" }) { const cls = { green: "bg-emerald-50 text-emerald-500", blue: "bg-blue-50 text-blue-600", orange: "bg-orange-50 text-orange-500" }; return <div className={`rounded-2xl p-3 ${cls[tone]}`}><div className="text-sm font-semibold">{label}</div><div className="mt-2 text-xl font-bold">{value} <span className="text-xs font-medium text-slate-400">/ {goal}{unit}</span></div><div className="mt-2"><ProgressBar value={value / goal * 100} color={tone === "green" ? "green" : tone} /></div></div>; }
