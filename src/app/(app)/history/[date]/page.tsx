"use client";

import Link from "next/link";
import { CalendarDays, ChevronRight, CircleCheck, Dumbbell, Droplets, Flame, Wallet } from "lucide-react";
import { useParams } from "next/navigation";
import { mockToday } from "@/mock/data";
import { BackLink, Badge, SectionHeader, Timeline } from "@/components/ui";

export default function HistoryDetailPage() { const params = useParams<{ date: string }>(); const date = params.date ?? "2026-08-17"; return <><div className="flex items-center justify-between pt-4 md:pt-8"><div className="flex items-center gap-1"><BackLink href="/history" /><div><div className="eyebrow">Read mode</div><h1 className="text-2xl font-bold">8 月 {date.slice(-2)} 日 · 周一</h1></div></div><Link href="/history" className="secondary-button px-3"><CalendarDays size={18} />日历</Link></div><div className="mt-2 flex items-center gap-2 pl-10 text-sm text-slate-500"><Badge tone="green">92% 完整</Badge><span>这一天记录得很不错</span></div><div className="app-card mt-5 grid grid-cols-5 divide-x divide-slate-100 p-4 text-center"><DayStat icon={<Flame />} label="热量" value="2054" tone="orange" /><DayStat icon={<span>◒</span>} label="蛋白质" value="123g" tone="purple" /><DayStat icon={<Droplets />} label="饮水" value="2400" tone="blue" /><DayStat icon={<Dumbbell />} label="训练" value="56min" tone="purple" /><DayStat icon={<Wallet />} label="支出" value="¥72" tone="orange" /></div><div className="mt-6"><SectionHeader title="完整时间线" action={<button className="text-sm font-semibold text-blue-600">编辑</button>} /><div className="app-card p-5"><Timeline items={mockToday.timeline} /></div></div><Link href={`/review/${date}`} className="primary-button mt-5 w-full"><CircleCheck size={18} />查看每日复盘 <ChevronRight size={18} /></Link></> }
function DayStat({ icon, label, value, tone }: { icon: React.ReactNode; label: string; value: string; tone: "orange" | "purple" | "blue" }) { const colors = { orange: "text-orange-500 bg-orange-50", purple: "text-violet-600 bg-violet-50", blue: "text-blue-600 bg-blue-50" }; return <div><div className={`mx-auto flex h-9 w-9 items-center justify-center rounded-xl ${colors[tone]}`}>{icon}</div><div className="mt-2 text-[11px] text-slate-500">{label}</div><div className="mt-1 text-xs font-bold">{value}</div></div>; }
